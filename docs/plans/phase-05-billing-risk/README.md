# Phase 5 Plans

This directory stores work-package plans for `Phase 5: Entitlements + Guest Trial + Billing + Risk`.

Use filename format `NN-short-name.md` and do not mix unrelated UX-only work into this phase.
